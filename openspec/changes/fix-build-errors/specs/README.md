# fix-build-errors — Specs

No new or modified capabilities. This is a maintenance-only change:

- **Type error fix**: `CollaboratorsSection.astro` — 4 lines, type assertion
- **Deprecation fixes**: `content.config.ts`, `ContactForm.tsx`, blog pages — import and type changes

No spec-level requirement changes — all fixes are type-system only with zero runtime behavior impact.
