# Category stamps — 16x16

One per project category, filenames matching the category slug:
`web-app.png`, `api.png`, `tool.png`, `open-source.png`.

These read as rubber stamps on an evidence card, so a slightly rotated, ink-worn look
suits them — but keep the silhouette readable at 16px. They sit on `--color-surface`.
